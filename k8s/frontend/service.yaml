apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: uphill-prod
spec:
  type: ClusterIP
  selector:
    app: uphill-frontend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP

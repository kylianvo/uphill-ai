[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: requires a live Postgres database (uphill_ai_test)
    kafka: requires a live local Kafka broker (docker compose up -d kafka)

fastapi>=0.110.0
uvicorn>=0.15.0
pydantic>=2.0.0
python-dotenv>=1.0.0
fitparse>=1.2.0
gpxpy>=1.6.2
google-genai>=2.0.0
python-multipart
pypdf
beautifulsoup4
youtube-transcript-api
httpx
# PostgreSQL + SQLAlchemy
psycopg2-binary>=2.9.0
# <2.1: SQLAlchemy 2.1 maps plain postgresql:// URLs to psycopg 3, which isn't installed
sqlalchemy>=2.0.0,<2.1
alembic>=1.13.0
# Auth
passlib[bcrypt]>=1.7.4
PyJWT>=2.8.0
# Encrypts third-party OAuth tokens at rest (services/token_crypto.py). Already
# present transitively; pinned explicitly so a transitive change can't remove it.
cryptography>=42.0.0
# NotebookLM
notebooklm-py>=0.7.0
# Web search (gear KB discovery)
tavily-python>=0.5.0
# Telemetry
prometheus-client>=0.20.0
prometheus-fastapi-instrumentator>=7.0.0
# KB vector retrieval
qdrant-client>=1.10.0
# Race course matching
rapidfuzz>=3.0.0
# Kafka clickstream pipeline
confluent-kafka>=2.3,<3.0
duckdb
# duckdb doesn't declare this as a hard dependency, but its SQL engine
# lazily imports it for timezone-aware operations (services/warehouse_extractor.py's
# incremental extraction hits this) -- without it, duckdb raises at query time
# rather than at import time, so the gap doesn't surface until a specific query runs.
pytz
# NOTE: dbt-duckdb is deliberately NOT listed here. It was originally kept out
# because dbt-core's protobuf>=6.0 requirement conflicted with the (now-removed)
# google-generativeai package's protobuf<6.0 pin -- google-genai (its replacement)
# has no protobuf dependency, so that specific conflict is gone, but
# scripts/run_dw_elt.py still runs dbt from an isolated venv at ./warehouse/.venv
# and hasn't been re-evaluated for merging back into this file's dependency tree.
# LLM observability -- imported ONLY by services/observability.py, lazily, so a stale
# prod image without these degrades to "observability disabled" instead of crashing.
# Exact pins: requirements.txt gaps have taken prod down three times, and these pull in
# the OpenTelemetry stack. openinference-instrumentation-google-genai 1.4.7 requires
# google-genai>=2.0.0. See docs/superpowers/specs/llm-observability-design.md.
langfuse==4.15.3
openinference-instrumentation-google-genai==1.4.7
# Second OpenInference instrumentor: the chat agent (backend/services/coach_chat.py)
# runs on LangGraph for provider-agnosticism (roadmap decision 16/17), so it needs its
# own auto-instrumentation alongside the google-genai one that covers Plan Gen/Gear/
# Nutrition's direct Gemini calls. langchain_core is declared as an optional "instruments"
# extra upstream, but the package's own __init__.py imports it unconditionally, so it is
# a real runtime dependency here -- pinned explicitly, not left to the extra.
openinference-instrumentation-langchain==0.1.76
langchain-core==1.6.3
langgraph==1.2.11
langchain-google-genai==4.4.0
opentelemetry-api==1.44.0
opentelemetry-exporter-otlp-proto-http==1.44.0
opentelemetry-sdk==1.44.0
